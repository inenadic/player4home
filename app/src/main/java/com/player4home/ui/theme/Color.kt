package com.player4home.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds (deep-space dark, like IPTV Smarters Pro) ─────
val NavyBackground     = Color(0xFF060911)   // near-black base
val NavySurface        = Color(0xFF0A1220)   // left panel / top bars
val NavySurfaceVariant = Color(0xFF0F1928)
val NavyCard           = Color(0xFF111B2D)   // list rows
val NavyCardElevated   = Color(0xFF172338)

// ── Primary accent (brighter teal) ────────────────────────────
val TealPrimary        = Color(0xFF00D4AA)
val TealPrimaryDark    = Color(0xFF00A07E)
val TealContainer      = Color(0xFF002E22)   // selected group background
val TealGlow           = Color(0x1A00D4AA)

// ── Secondary (amber/gold for FILE playlist type) ─────────────
val AmberSecondary     = Color(0xFFFFB300)
val AmberSecondaryDark = Color(0xFFFF8F00)
val AmberContainer     = Color(0xFF2D1E00)
val AmberGlow          = Color(0x1AFFB300)

// ── Text ──────────────────────────────────────────────────────
val OnNavy             = Color(0xFFEBF0F8)
val OnNavyVariant      = Color(0xFF687D96)
val OnNavySubtle       = Color(0xFF2A3A4E)
val OnTeal             = Color(0xFF001A12)
val OnAmber            = Color(0xFF1A1000)

// ── Status ────────────────────────────────────────────────────
val ErrorColor         = Color(0xFFFF4F4F)

// ── Stream type badges (aligned with home-screen tile colors) ─
val LiveBadge          = Color(0xFF00D4AA)   // teal  — Live TV tile
val VodBadge           = Color(0xFFFF6A00)   // orange — Movies tile
val SeriesBadge        = Color(0xFF8B5CF6)   // purple — Series tile

// ── Borders & dividers ────────────────────────────────────────
val Divider            = Color(0xFF141E2E)
val CardBorder         = Color(0xFF192840)
